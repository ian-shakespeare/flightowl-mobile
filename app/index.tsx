import { StyledView, StyledText } from '../components/StyledElements'
import Spacer from '../components/UI/Spacer'

const Home = () => {
    return (
        <StyledView className='flex-1 items-center justify-center'>
            <StyledText>Home page</StyledText>
            <Spacer />
        </StyledView>
    )
}

export default Home
